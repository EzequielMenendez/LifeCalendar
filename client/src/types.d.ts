export interface LoginUser {
    email: string,
    password: email
}

export interface RegisterUser{
    name: string,
    email: string,
    password: email
}

export interface UserData {
    email: string;
    name: string;
    id: string;
}